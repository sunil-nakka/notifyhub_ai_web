# Optional. When set, the contact and interest forms POST JSON to this URL.
# When unset, they fall back to composing an email to the address in
# src/lib/constants.ts so a submission is never silently discarded.
NEXT_PUBLIC_CONTACT_ENDPOINT=
